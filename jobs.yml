- name : Ansible Job Create
  hosts :
  tasks:
    - name : Create Jenkins Jobs
      community.general.jenkins_job:
        config: "{{ lookup('file', 'job.xml') }}"
        name: test1
        password: Admin123
        url: http:// 172.31.35.28:8080
        user: admin

